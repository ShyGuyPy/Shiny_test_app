tabPanel("box",
         #plotOutput(outputId = "ibox")

dashinfoBox(title, value = NULL, subtitle = NULL,
        icon = shiny::icon("bar-chart"), color = "aqua", width = 4,
        href = NULL, fill = FALSE)
)